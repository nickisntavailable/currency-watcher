# Локально: docker-compose (см. docker-compose.yml).
# В проде (Railway): подставляется автоматически через reference-переменную
# ${{Postgres.DATABASE_URL}}, когда сервис приложения связан с сервисом Postgres.
DATABASE_URL="postgresql://currency:currency@localhost:5432/currency_converter"
